# /test-feature — Fast Feature Test Feedback

Run unit and integration tests only for fast feedback during development:

1. **Unit tests** — `npm run test:unit`
2. **Integration tests** — `npm run test:integration`

Skip E2E tests (slower, need browser) for quick iteration.
Report any failures immediately with file paths and line numbers.
